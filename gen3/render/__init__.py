"""Render intent definitions for Gen 3 downstream generators.

Render intent is presentation state, not Character DNA. It describes what a
downstream image generator should show while leaving the character itself
unchanged.
"""
